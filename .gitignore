.env
.env.*
!.env.example
.vscode/**
.idea/**
**/.DS_Store
Thumbs.db

# Local runtime / secrets
storage/
fslog/
data/
web/.launcher-hint.json

# Machine-specific workspace config (see docs/examples/workspace-config.json)
# app-metadata: workspace-config
.mdular/config.json
# Legacy local config from the previous application identity (never commit)
.mdtk/config.json

# User-generated workspace content (do not publish)
Chat.md
issues/
journal/
archive/

# Future TS toolchain
node_modules/
dist/
*.tsbuildinfo

# Tauri build output
src-tauri/target/
latest.json
*.sig

# Updater / code signing (private key only — public key lives in tauri.conf.json)
.tauri/
*.key
!*.key.pub
*.pem

# OS / editor
*.log

# Cursor local state
.cursor/projects/
.cursor/*.json
.cursor/rules/

# AI / local dev notes (keep on your machine, not for GitHub)
PLAN.md
PROGRESS.md
MEMORY.md
